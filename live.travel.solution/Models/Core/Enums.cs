namespace live.travel.solution.Models.Core {
    public enum MsgType {
        Success = 0,
        Error = 1,
        Info = 2,
    }

    public enum PlanType {
        Gold = 1,
        Platinum = 2,
    }

    public enum FormStatus {
        New = 0,
        Prospected = 1,
        Associated = 2,
    }
}
